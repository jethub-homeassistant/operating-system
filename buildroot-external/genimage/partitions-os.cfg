include("image-spl-spl.cfg")

		partition spl {
		offset = 0
		size = ${BOOT_SPL_SIZE}
		image = "spl.img"
		in-partition-table = "no"
		holes = {"(440; 512)"}
	}

	partition os-reserved {
		offset = 8M
		size = 120M
	}

	partition os-bootstate {
		offset = 129M
    	size = ${BOOT_ENV_SIZE}
		partition-type = 0x83
	}

	partition os-systemA {
		offset = 132M
		size = ${SYSTEM_SIZE}
		image = ${SYSTEM_IMAGE}
	}

	partition os-systemB {
		offset = 389M
		size = ${SYSTEM_SIZE}
		image = ${SYSTEM_IMAGE}
	}

	partition os-overlay {
		offset = 646M
    	size = ${OVERLAY_SIZE}
		partition-type = 0x83
    	image = "overlay.ext4"
	}

	partition os-reserved_end {
		offset = 711M
    	size = 313M
    	partition-type = 0x83
	}