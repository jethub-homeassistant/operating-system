#!/bin/sh
# ==============================================================================
# HA utility
# ==============================================================================

if [ -t 1 ]; then
	# stdout (fd 1) must be terminal, otherwise `-t` causes error
	docker exec -it jhio_cli ha "$@"
else
	docker exec -i jhio_cli ha "$@"
fi
